---
layout: main

---

<h3 class="text-lg md:text-xl font-semibold uppercase">{{ title }}</h3>

<div class="flex flex-row gap-x-2">
  {% for tag in tags %}
    <main-tag name={{tag}}></main-tag>
  {% endfor %}
</div>

<div class="text-sm md:text-lg">
  {{ content | safe }}
</div>